default: &default
  adapter: postgis
  encoding: unicode
  username: postgres
  password: password
  database: vedoc-api_development
  host: localhost
  pool: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } %>

development:
  <<: *default


test:
  <<: *default
  database: vedoc-api_test

production:
  <<: *default
